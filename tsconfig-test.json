{
  // there is no way to do partial overriding extended tsconfig.json
  // since TypeScript doesn't do merge, it does overriding https://github.com/Microsoft/TypeScript/pull/14178
  "compilerOptions": {
    "strict": true,
    "lib": [
      "es2015"
    ],
    "baseUrl": ".",
    "typeRoots": [
      "./node_modules/@types",
      "./src/@types"
    ]
  }
}
